osuild manifests for various hardward and virtualization platforms

use the do_osbuild.sh script from the above directory to create images:

./do_osbuild osbuild-manifests/rhel8-build-x86_64.mpp.json --export assembler
